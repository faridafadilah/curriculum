import { corsConfig } from './cors.config';
import dataSource, { dataSourceOptions } from './database.config';
import { swaggerConfig } from './swagger.config';

export { corsConfig, dataSourceOptions, dataSource, swaggerConfig };
